import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import { VitePWA } from 'vite-plugin-pwa';
export default defineConfig({ base: './', test: { exclude: ['e2e/**', 'node_modules/**', 'dist/**'] }, build: { rollupOptions: { output: { manualChunks: (id) => id.includes('node_modules/recharts') ? 'charts' : id.includes('node_modules/jspdf') ? 'pdf' : id.includes('node_modules/html2canvas') ? 'canvas' : id.includes('node_modules/xlsx') ? 'xlsx' : id.includes('node_modules/dexie') ? 'db' : id.includes('node_modules/react') ? 'react' : undefined } } }, plugins: [react(), VitePWA({ registerType: 'autoUpdate', manifest: { name: 'e-PKPM', short_name: 'e-PKPM', description: 'Sistem Penilaian Kinerja Pengawas Madrasah', theme_color: '#102a4c', background_color: '#ffffff', display: 'standalone', start_url: './', icons: [{ src: 'icon.svg', sizes: 'any', type: 'image/svg+xml', purpose: 'any maskable' }] }, workbox: { globPatterns: ['**/*.{js,css,html,svg,png}'], navigateFallback: 'index.html' } })] });
